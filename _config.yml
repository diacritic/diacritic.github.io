title: Diacritic
tagline: ""
url: "https://snai.pe" # The base hostname & protocol for your site. Use it if you need absolute URL's
permalink: /blog/:title.html

github: Snaipe

defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "article"

sass:
  sass_dir: css/
  style: :compressed

exclude: [.scss, README.md, node_modules, gruntfile.js, package.json]
